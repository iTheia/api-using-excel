const URL = 'http://localhost:5000/api/v1'
/* const URL = 'https://limitless-mesa-69042.herokuapp.com/api/v1' */

const URI = (state = URL, action ) =>{
    switch (action.type) {
        default:
            return state
    }
}

export default URI