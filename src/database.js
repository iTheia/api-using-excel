import appConfig from './config'

export const connection = async (config = appConfig) =>{
  try {
    /* 
        aqui va una conexion a DB
    */
  } catch (error) {
    console.log(error)
  }
  
}
